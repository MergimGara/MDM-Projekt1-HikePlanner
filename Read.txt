Rolle & Kontext:
Du bist ein absoluter Senior Full-Stack Entwickler und MLOps-Experte. Mein Ziel ist es, in meinem Hochschulmodul "Model Deployment & Maintenance" (MDM) an der ZHAW die Bestnote 6.0 zu erreichen. Dafür muss mein Projekt "HikePlanner" qualitativ herausragend sein.

Mein aktueller Tech-Stack:

Backend: Python, Flask, Pandas, Scikit-Learn. Es lädt ein Machine-Learning-Modell aus dem Azure Blob Storage und bietet eine /api/predict Schnittstelle.

Frontend: SvelteKit.

Automatisierung: GitHub Actions (CI/CD) für Linting, Testing und Deployment als Docker-Container auf Azure App Services.

Deine Aufgabe:
Implementiere zwei offizielle "Bonus-Themen", um die maximalen Bonuspunkte zu sichern. Die Vorgabe der Uni lautet: "Ausschliesslich für herausragende und besonders aufwändige Zusatzarbeiten". Schreibe Code auf absoluten Top-Niveau (Clean Code, kommentiert, robust, Edge-Cases abgedeckt).

Gehe folgende zwei Bonus-Themen nacheinander an und gib mir den exakten, vollständigen Code für die anzupassenden Dateien:

Bonus-Thema 1: Automatisierung ModelOps / CI/CD Pipeline ausbauen (Fast fertig)
Ich habe bereits eine .github/workflows/ci.yml und grundlegende Tests in backend/test_app.py. Das muss jetzt auf ein 6er-Niveau gehoben werden.

CI-Pipeline fixen & erweitern: Aktuell führt ci.yml nur pytest backend/test_formulas.py aus. Ändere das, sodass alle Tests (inkl. Integration Tests) laufen (pytest backend/).

Integration Tests perfektionieren (test_app.py): - Schreibe umfassende Integration-Tests für meine Flask-App.

WICHTIG: Da die Tests in den GitHub Actions laufen, haben sie dort keinen Zugriff auf den echten Azure Blob Storage. Schreibe einen professionellen Mock (z.B. mit unittest.mock.patch), der den Download der GradientBoostingRegressor.pkl und LinearRegression.pkl simuliert, damit die Pipeline nicht abstürzt!

Teste Edge-Cases (z.B. negative Inputs für uphill/downhill, fehlende Parameter, fehlerhafte JSON-Responses).

Bonus-Thema 2: Erweitertes Frontend (SvelteKit UI Premium-Upgrade)
Mein SvelteKit-Frontend funktioniert, ist aber noch zu simpel. Mache daraus ein "Erweitertes Frontend", das den Dozenten umhaut.

Design & UX: Integriere modernes Styling (gerne reines CSS oder ein leichtes Framework, falls in SvelteKit einfach integrierbar, ansonsten bestes Custom CSS). Das UI muss responsiv, sauber und professionell wirken (Transitions, Hover-Effekte).

Interaktive Features: Implementiere visuelle Highlights. Wenn das Backend die verschiedenen Zeiten (Gradient, Linear, DIN33466, SAC) zurückgibt, zeige diese nicht nur als Text, sondern visuell ansprechend an (z.B. als animierte Balken, schöne Cards oder einen interaktiven Chart).

State Management & Error Handling: Implementiere einen professionellen Loading-State (Spinner/Skeleton) während der API-Request läuft, sowie ein schönes Error-Handling, falls das Backend mal nicht erreichbar ist.

Output-Regeln für dich:

Analysiere kurz, wie du an die Umsetzung herangehst.

Gib mir danach für jede Datei, die geändert werden muss (z.B. ci.yml, test_app.py, frontend/src/routes/+page.svelte, frontend/static/styles.css), den kompletten und copy-paste-fertigen Code.

Achte auf absolute Fehlerfreiheit. Ich muss den Code direkt deployen können.